<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true" android:drawable="@color/green" /> <!-- pressed -->
    <item android:state_focused="true" android:drawable="@color/night" /> <!-- focused -->
    <item android:state_hovered="true" android:drawable="@color/night" /> <!-- hovered -->

    <!-- Not using ?attr/themeColor as that is only supported on > API 21 -->
    <item  android:drawable="@color/night" />  <!-- default -->
</selector>